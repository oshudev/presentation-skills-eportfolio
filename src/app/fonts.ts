import { Manrope, Space_Mono } from 'next/font/google';

export const manrope = Manrope({ subsets: ['latin'], weight: '500' });

export const spaceMono = Space_Mono({
  subsets: ['latin'],
  weight: '700',
});
